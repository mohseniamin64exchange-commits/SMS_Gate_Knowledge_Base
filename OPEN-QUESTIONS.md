# داده‌هایی که هنوز باید با مالک پروژه قطعی شوند

اینها مانع ثبت دانش موجود نیستند، اما برای release/restore عملی لازم‌اند:

1. APK فعلی نصب‌شده دقیقاً از کدام branch/commit ساخته شده و SHA-256 آن چیست؟ صفحهٔ راهنمای عمومیِ مشاهده‌شده با routeهای `main@474bf21` منطبق نیست.
2. آیا آخرین rebuild محلی بر پایهٔ capcom6 باید به مخزن اصلی منتقل شود یا کنار گذاشته شود؟
3. قرارداد نهایی transport چیست: HTTP محلی پیش‌فرض یا HTTPS؟ شرایط شبکهٔ مجاز، هشدار کاربر و مدل تهدید باید مشخص شوند.
4. پنل وب نهایی در کدام repo و نسخه است؟ backend موجود در سورس اصلی فقط Call Log API مستقل دارد.
5. استاندارد ستون‌های Excel و normalization شماره پس از آزمون اولیه هنوز قطعی نشده است.
6. محل امن backup signing key، credential و دیتابیس عملیاتی کجاست؟ مقدار secret در این سند ثبت نشود.
7. دستگاه/Androidهای هدف و نتیجهٔ آزمون ۷۲ساعته هنوز ثبت نشده‌اند.

هنگام پاسخ به این پرسش‌ها، این فایل را به‌روزرسانی کنید و تصمیم نهایی را در `DECISIONS.md` ثبت کنید.
