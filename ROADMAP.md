# نقشه‌راه از شروع تا ادامه

## مبدا

پروژه از یک پنل وب برای کار با SMSGate شروع شد: ارسال گروهی از اکسل، شماره‌های دارای صفر اول یا بدون آن، پیام‌های ورودی/خروجی، وب‌هوک، وضعیت صف، نمایش شمسی و تنظیمات پیشرفته. استاندارد نهایی فایل Excel عمداً به گفت‌وگوی بعدی موکول شد. تاریخ‌ها در دیتابیس میلادی ذخیره و در UI شمسی نمایش داده می‌شوند؛ ورودی شمسی باید پیش از ذخیره تبدیل شود.

مشکل تعیین‌کننده این بود که SMSGate پس از حذف مکالمه از گوشی همچنان پیام‌های قدیمی را به پنل برمی‌گرداند. پاک‌کردن آرشیو پنل و restart مشکل را حل نکرد؛ منبع بازخوانیِ SMSGate هنوز داده را برمی‌گرداند. راه‌حل‌های موقتی مثل حذف محلی کافی نبودند. تصمیم گرفته شد اپ Android مستقل، SMS Provider را مستقیم بخواند و حذف واقعی را تشخیص دهد.

## مرحله‌های انجام‌شده در سورس Android

- [x] ساخت پروژه Kotlin/Compose/Room/Retrofit و CI.
- [x] خواندن واقعی Inbox و Sent از Provider و تفکیک direction.
- [x] snapshot کامل، tombstone و تست برای جلوگیری از بازگشت پیام حذف‌شده.
- [x] صف ارسال، requestId، SIM، multipart و delivery callback در کد.
- [x] UI فارسی RTL با مکالمه‌ها، مخاطبین، تماس‌ها، Gateway، Settings و Back مرحله‌ای.
- [x] Call Log محلی و backend مستقل برای sync تماس‌ها.
- [x] Phone-as-Server با TLS، API auth و پورت پیش‌فرض ۳۰۳۰ در `main` آنلاین.
- [x] اصلاح خطای JVM target و assertion قدیمی Robolectric.
- [ ] تأیید همهٔ موارد بالا روی گوشی واقعی و مرورگرهای هدف.

## وضعیت محصول در زمان بکاپ

SMSGate خارجی نباید وابستگی نهایی باشد. سورس آنلاین Phone Server را دارد، اما نسخهٔ نصب‌شده روی گوشی و سورس آنلاین لزوماً یکی نیستند. ورود به ریشهٔ آدرس در نسخهٔ نصب‌شده صفحهٔ راهنما نشان داده، حال آنکه کد `PhoneHttpServer.kt` در commit مبنا route ریشه ندارد. HTTP پیش‌فرض/HTTPS پیشرفته خواستهٔ اخیر است و تا این تاریخ **پیاده‌سازی و انتشار آن تأیید نشده**.

## اولویت‌های بعدی

1. commit/branch و SHA-256 APK نصب‌شده را ثبت و با سورس تطبیق بده.
2. تجربهٔ اتصال مرورگر را تعیین تکلیف کن: HTTP محلیِ ساده یا HTTPS با راه‌اندازی اعتماد گواهی. این تصمیم نیازمند مرور تهدید LAN است.
3. اتصال یک‌دکمه‌ای با نمایش IP/Port و خطای قابل فهم را روی گوشی واقعی آزمایش کن.
4. تست واقعی SMS: ورودی، خروجی، حذف، restart، ۱۷هزار+ پیام، دو سیم، delivery و خطای مجوز.
5. تست Call Log، نام مخاطب و حریم خصوصی؛ فقط گزارش تماس، نه انتقال صدا.
6. Build/CI، تست امنیت، باتری/Doze/Boot و اجرای طولانی.
7. ساخت پنل وب سازگار با قرارداد Phone Server و release امضاشده.

## معیار نسخهٔ اولیهٔ قابل نصب

Build سبز با SHA مشخص، تست خودکار سبز، APK قابل انتساب به همان commit، اتصال PC و گوشی در LAN، خواندن/ارسال واقعی SMS، حذف بدون resurrection، Call Log واقعی، Back و مجوزها، راهنمای اتصال برای کاربر عادی و گزارش محدودیت‌های باقیمانده.
